package com.restaurantmanager.entity;

/** A curated set of brand color presets an admin can pick for their dashboard and guest ordering pages. */
public enum ThemeColor {
    ORANGE,
    GREEN,
    BLUE,
    PURPLE,
    ROSE,
    TEAL
}
